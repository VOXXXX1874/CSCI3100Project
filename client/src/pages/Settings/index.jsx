import React from 'react';

// Abandon the settings page, the settings is integrated into the the header
export default function SettingsPage(){
    return(
        <div className="SettingsPage">
            <h2>This is settings page</h2>
        </div>
    );
}